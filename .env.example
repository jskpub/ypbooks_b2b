# 알라딘 프록시(Cloudflare Worker) 주소. 실제 TTBKey는 이 값에 없고 Worker Secret로만 존재하므로
# 이 값 자체는 비밀이 아니다 — 그대로 복사해서 쓰면 된다.
#   cp .env.example .env
VITE_ALADIN_PROXY_URL=https://ypbooks-b2b-aladin-proxy.ypbookb2b.workers.dev
